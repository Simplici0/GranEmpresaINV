# Configuración histórica

Estos archivos se conservan para referencia histórica. No describen el
servidor actual y no deben instalarse en producción.

El entorno activo usa `stocki`, `/opt/stocki`, ARM64, Nginx y el puerto 8090.
